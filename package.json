{
  "name": "test-task-grandysoft",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "migrate:latest": "npx knex migrate:latest --knexfile src/db/knexfile.js --migrations-directory migrations",
    "migrate:down": "npx knex migrate:down --knexfile src/db/knexfile.js",
    "seed": "npx knex seed:run --knexfile src/db/knexfile.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "knex": "^2.3.0",
    "pg": "^8.8.0"
  },
  "devDependencies": {
    "editorconfig": "^1.0.1",
    "nodemon": "^2.0.20"
  }
}
